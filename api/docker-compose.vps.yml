# ⚠️ ARQUIVO DESCONTINUADO (2026-07-16)
#
# Este arquivo continha secrets hardcoded versionados no git (POSTGRES_PASSWORD,
# KESTRA_PASSWORD, PGADMIN_DEFAULT_PASSWORD) — prática removida.
# As senhas que estavam aqui devem ser consideradas COMPROMETIDAS e rotacionadas.
#
# O compose de produção canônico é ../docker-compose.prod.yml (raiz do repo),
# com todos os secrets via variáveis de ambiente (Portainer/.env).
# Guia de deploy: ../DEPLOY_VPS.md
#
# Nota preservada do arquivo original: a VPS já tem Caddy + rede proxy_net
# (domínios *.fulled.com.br) — reaproveitar para HTTPS do dashboard (ver guia).
